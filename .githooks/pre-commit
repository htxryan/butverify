#!/bin/sh
set -eu

doc_path="marketing-site/src/content/docs/docs/reference/cli.md"

task docs:generate

if ! git diff --quiet -- "$doc_path"; then
	git add "$doc_path"
	printf '%s\n' "Generated CLI docs changed and were staged. Review them, then rerun commit."
	exit 1
fi
